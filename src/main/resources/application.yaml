spring:
  application:
    name: teste-softplan
  profiles:
    default: prod
  jpa:
    open-in-view: false

server:
  port: 8080

app:
  security:
    basic:
      username: ${USERNAME}
      password: ${PASSWORD}
